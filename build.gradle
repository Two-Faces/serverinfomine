plugins {
    id 'java'
}

group = 'ru.bifacial.serverinfo'
archivesBaseName = 'ServerInfo'
version = '1.0.0'

sourceCompatibility = '1.8'
targetCompatibility = '1.8'

compileJava.options.encoding = 'UTF-8'

repositories {
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    compile name: "bukkit-1.7.10"
}
